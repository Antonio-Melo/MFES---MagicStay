class Review
instance variables
	user: User;
	review: seq of char;
operations
	public Review: User * seq of char ==> Review
	Review(u, r) == (
		user := u;
		review := r;
	);
	--TODO
		-- Add postconditions
	public getUser: () ==> User			
	getUser() == return user;
	
	public getReview: () ==> seq of char 
	getReview() == return review;
end Review